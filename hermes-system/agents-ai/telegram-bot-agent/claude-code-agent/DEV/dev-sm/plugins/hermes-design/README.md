# hermes-design
Pair with (install separately, both are near-mandatory):
- `/plugin install frontend-design@claude-plugins-official` — Anthropic's anti-slop skill (~400 tokens, top installed plugin of the ecosystem)
- Figma Dev Mode MCP (official, only if you have Figma files): `claude mcp add --transport http figma https://mcp.figma.com/mcp`
Optional component sources: shadcn MCP, Magic MCP (21st.dev). Add per-project, not globally — MCP tools cost context.
