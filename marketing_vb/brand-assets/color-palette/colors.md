# 3DLOOK — Color Palette  ⚠️ SUPERSEDED

> **This file is superseded. Do not use the values below.**
> The single source of truth for all brand colors is **`DESIGN.md`** (repo root) —
> confirmed from the official Figma export.
>
> Canonical values now:
> - **Electric blue accent:** `#143DFF` (NOT the old `#2962FF` placeholder)
> - **Navy surfaces:** `#050F40`
> - Full blue / gray / neutral 10-step scales, usage weighting, navy glow gradient,
>   status-color caveat, and a copy-paste `:root` CSS block — all in `DESIGN.md` §2 and §13.
>
> This file was an approximate placeholder created before the Figma export existed.
> It is kept only so old path references resolve. **Agents: read `DESIGN.md` instead.**

Historic placeholder retained for reference only:

- 3DLOOK Blue: `#2962FF` — **WRONG / superseded → use `#143DFF` from DESIGN.md**
- Fonts note lived in `fonts.md` — also superseded → see `DESIGN.md` §3 (Satoshi).
